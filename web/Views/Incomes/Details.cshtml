@model web.Models.Income

@{
    ViewData["Title"] = "Details";
}

<div class="card p-3 mb-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h4 m-0">Income details</h1>
        <a class="btn btn-sm btn-outline-secondary" asp-action="Index">Back</a>
    </div>
    <hr />
    <div>
        <h4>Income</h4>
        <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Amount)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Amount)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Date)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Date)
        </dd>
    </dl>
        </div>
        <div class="mt-3">
            <a class="btn btn-sm btn-outline-primary me-1" asp-action="Edit" asp-route-id="@Model?.Id">Edit</a>
            <a class="btn btn-sm btn-outline-secondary" asp-action="Index">Back</a>
        </div>
    </div>
